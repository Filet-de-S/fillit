# **************************************************************************** #
log:
16.05: added add.map;

OUR FUNCTIONS:
1. main.c = open file, check for valid, if ok, it'll call gogogo.c
			and send to it our list;

2. gogogo.c = at the moment nothing;
3. extra.c =
			void.friwka – for main.c to free in error case;
			void.ft_lstaddendfil – to place new node in the end of list;
			char**.add_map – returns MAP** with sizeXsize & filled by dots;
			char**.size_plus – returns existing map with expanded lines;
4. fillit.c =
			

*T = tetramino;
PLACE = place in first available mtrx;

ALGO:
    1. place; 
	2. save the result: count the square size and free holes, consider *T's appearance;


	1. 
	a) if map doesn't exists, create by the size of 1st *T, then place it.
	b) place next, if doesn't fit, try next till end, if not, place 2nd as 1st: REP.
		if NOT & NOT, +sizeMAP, and again (1.a);

	2. 
	a) save the result: SIZExSIZE, free holes, UPLEFT pos. + *T's order.
	b) do all possible variations by (1), save when is better than prev: !map++;
	c) will stop when all var. are tried.

	3. print;

	0) созд. карты
	1) функция проверки на возможность добавления новой Т/
	2) функиця добавления новой Т, по возм. СЛЕВА ВВЕРХ/
	3) 
